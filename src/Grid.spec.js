import Grid from "Grid";
import config from "config";
import Point from "Point";

describe("Grid", () => {
	describe("blocksInRect", () => {
		it("should return", () => {
			let grid = new Grid();
		});
	});
});
